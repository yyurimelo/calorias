# URL do backend (Express). Usada pelo proxy do Next.js (next.config.ts)
# para encaminhar /api/* até o servidor de análise.
# Não exponha chaves aqui — este arquivo é versionado.
BACKEND_URL=http://localhost:3001
